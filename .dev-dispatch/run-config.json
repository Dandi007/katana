{
 "development_id": "dev-fg-6abec86fda07",
 "generation": 1,
 "acceptance_commands": [
  [
   "bash",
   "-lc",
   "V=\"$(mktemp -d)\"; python3 -m venv \"$V/venv\" && \"$V/venv/bin/pip\" install -q -e mcp/shared -e mcp/kernel -e mcp/work-folder pytest && \"$V/venv/bin/python\" -m pytest -q mcp/work-folder/tests --import-mode=importlib -p no:cacheprovider"
  ]
 ],
 "setup_commands": [],
 "acceptance_env": {}
}
