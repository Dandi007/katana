skill: work-folder:checkpoint
setup:
  fixture: kb
trigger:
  prompt: |
    用 /work-folder:checkpoint 说明你将如何从 opaque folder_id `wf-a1b2c3`
    执行一次 MCP-only Resume。
    这是 contract reasoning case，未连接真实 Work Folder MCP，请给出会使用的
    公开 tool 名、folder_id/filename 参数边界、server-side 环境验证、
    MATCH/DRIFT/BROKEN 分流与持久化语义，不要执行 mutation。
    严禁用 native Read/Write/Edit/Glob/Bash 访问 Work Folder，也不要构造或暴露
    physical path。
  tools: []
expect:
  process:
    - skill_loaded: work-folder:checkpoint
    - tool_absent: Read
    - tool_absent: Write
    - tool_absent: Edit
    - tool_absent: Glob
    - tool_absent: Bash
  semantic:
    rubric: case-rubrics/checkpoint-resume-mcp.md
    inputs:
      answer: "{result_text}"
