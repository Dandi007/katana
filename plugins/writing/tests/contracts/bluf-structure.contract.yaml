skill: writing:bluf
setup:
  fixture: kb
trigger:
  prompt: "按 bluf 的信息架构：一份文档的 L0 应该写什么、必须是什么句式？L1 是什么？"
  tools: []
expect:
  process:
    - skill_loaded: writing:bluf
  semantic:
    rubric: ../../../../tests/judge/case-rubrics/writing-bluf-structure.md
    inputs:
      - "{case_trace}"
