skill: writing:using-writing
input:
  prompt: "根据本 session 已注入的 writing 约定：检查一份文档好不好读应该用哪个 skill、可读性冷读这一步必须用什么来执行、写文档前要先读哪个子目录"
  cwd: kb
assert:
  - stdout_grep: "writing:"
  - stdout_grep: "subagent"
  - stdout_grep: "patterns"
