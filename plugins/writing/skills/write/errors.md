# write 错误记录

## YYYY-MM-DD HH:MM - <错误简述>

**触发任务**：<任务或文档类型>
**症状**：<观察到的异常>
**影响**：<对写作流程的影响>
**根因分析**：<已知原因或待确认原因>
**处理状态**：open | mitigated | resolved
**后续动作**：<是否需要更新 skill 或 wrapper>

---

## 2026-03-19 01:06 - worktree 中准备实现但编辑落在主工作区

**触发任务**：writing-orchestrator skill 实现与验证
**症状**：已按流程创建隔离 worktree，但后续使用 `apply_patch` 时，文件实际写入了默认主工作区而不是新建 worktree
**影响**：隔离工作区与实际改动位置不一致，容易让验证读取错误路径，也会削弱 branch 隔离性
**根因分析**：当前编辑工具默认绑定主工作区，不能像 `bash` 一样显式指定 `workdir`，导致在多 worktree 场景下容易误写
**处理状态**：mitigated
**后续动作**：后续在 worktree 场景先验证编辑工具目标目录；必要时补充流程提示，避免把验证路径指向错误工作区

---
