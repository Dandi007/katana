skill: retrieval:search-note
input:
  prompt: |
    用 /retrieval:search-note 在当前知识库搜「意式浓缩 温度」，列出命中笔记。
  cwd: kb
run:
  timeout: 180
  allowed_tools: [Read, Grep, Glob, Bash]
assert:
  - stdout_grep: "意式浓缩温度-A"
  - stdout_grep: "意式浓缩温度-B"
