skill: memory:remember
setup:
  fixture: kb
trigger:
  prompt: "用 memory:remember 记住：本测试库的咖啡矛盾对是意式浓缩 92°C vs 96°C，两派互链不抹平；卡片写到 ./memory/"
  tools: [Read, Grep, Glob, Write, Edit, Bash]
expect:
  process:
    - skill_loaded: memory:remember
  filesystem:
    - created: "memory/*.md"
    - script: remember-card.verify.sh
    - unchanged_outside: true
